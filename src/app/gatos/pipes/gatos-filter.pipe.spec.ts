import { GatosFilterPipe } from './gatos-filter.pipe';

describe('GatosFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GatosFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
